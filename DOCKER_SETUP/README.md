# Publish to a Repo
```
sudo docker build -t <somename> .                                                 
sudo docker login                                                               
sudo docker tag <somename> <alejandrox1>/<repositoryname>:tag                         
sudo docker push <alejandrox1>/<repositoryname>:tag
```
